apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homepage-ip
  namespace: app-dev
spec:
  ingressClassName: traefik
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: homepage
            port:
              number: 3000
